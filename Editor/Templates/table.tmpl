using System;
using System.Collections.Generic;
using System.Linq;
using GameDBLibrary;

/**************************************************************************************
*
*
*                     THIS IS A GENERATED FILE! DO NOT EDIT!
*
*
**************************************************************************************/

namespace GameDB{0}
{{
    public class {1}Table : TableBase
    {{
        public {1}Table(Func<string, RowBase> rowFactory) : base({1}Schema.TableName, {2}, {3}, rowFactory) {{
            m_fields = new Dictionary<string, FieldBase>() {{
{4}
            }};
        }}

        public {1} GetByKey({5} key) {{ return m_data[{6}] as {1}; }}

        public bool TryGetByKey({5} key, out {1} row) {{ row = null; if (m_data.ContainsKey({6})) {{ row = m_data[{6}] as {1}; return row != null; }} return false; }}

        public Dictionary<{5}, {1}> GetRows() {{ return m_data.ToDictionary(entry => {7}, entry => ({1})entry.Value); }}
    }}
}}
